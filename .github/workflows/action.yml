name: test-actions
on:
  push:
jobs:
  myJob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v3.1.4
      - name: Install tool
        run: poetry add cyclonedx-bom 
      - name: Generate BOM
        id: sbom
        run: |
             cyclonedx-py --format xml - o sbom.xml
             
      - name: Submit SBOM
        run: |
          curl -k -X POST "https://dt-api.staging.cryptosoft.com/api/v1/bom" \
            -H "Content-Type: multipart/form-data" \
            -H "X-Api-Key:1DMIXAnGtTIeTQDddlHSvpj6d3as174S" \
            -F "autoCreate=true" \
            -F "projectName=python7test" \
            -F "projectVersion=1.2.2" \
            -F "bom=@sbom.xml"
